{
  "name": "blurt",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Live classroom quiz: questions on the projector, answers on phones.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/*.test.js",
    "verify:db": "node --env-file=.env.local scripts/verify-db.mjs",
    "load": "node --env-file=.env.local scripts/load-test.mjs",
    "check:migrations": "bash scripts/check-migrations.sh",
    "check:sql": "bash scripts/check-sql.sh"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.3.0",
    "svelte": "^5.57.0",
    "vite": "^8.3.0"
  },
  "dependencies": {
    "@fontsource-variable/rubik": "^5.3.0",
    "@fontsource/londrina-solid": "^5.3.0",
    "@supabase/supabase-js": "^2.116.0",
    "canvas-confetti": "^1.9.4"
  }
}
